<!--
================================================================================
画面遷移図 テンプレート(文書の骨組み)

- 本テンプレートは「タイトル + 図の骨組み + プレースホルダ」のみを定義する。
- 図の分け方・遷移の出し方・線のラベルの書き方 等の振る舞いルールは `.claude/skills/d2-screen-flow-diagram/SKILL.md` を参照。
- プレースホルダ `{{xxx}}` / 要確認マーカー / 文体 / ID 表記 等の汎用規約は リポジトリ直下の `CLAUDE.md` を参照。
================================================================================
-->

# {{アクター名: ACT-N}}

```mermaid
flowchart LR
    {{inN}}(["{{図の外からの入口}}"])

    subgraph {{分類N}}["{{画面分類}}"]
        {{SCRN}}["{{画面名}}<br/>{{SCR-N}}"]
        {{SCRM}}["{{画面名}}<br/>{{SCR-M}}"]
    end

    subgraph {{分類K}}["{{画面分類}}"]
        {{SCRK}}["{{画面名}}<br/>{{SCR-K}}"]
    end

    {{outN}}(["{{図の外への出口}}"])

    {{inN}} --> {{SCRN}}
    {{SCRN}} -->|"{{遷移の契機}}"| {{SCRM}}
    {{分類N}} -->|"{{遷移の契機}}"| {{SCRK}}
    {{SCRM}} -->|"{{遷移の契機}}"| {{outN}}
```
