<!--
================================================================================
C4モデル レベル1: システムコンテキスト テンプレート(文書の骨組み)

- 本テンプレートは「タイトル + 図の骨組み + 表の列構成 + プレースホルダ」のみを定義する。
- 図示の方針・描く対象の範囲・登場要素の選定・各列の埋め方 等の振る舞いルールは `.claude/skills/d2-c4-level1-system-context/SKILL.md` を参照。
- プレースホルダ `{{xxx}}` / 要確認マーカー / 文体 / ID 表記 等の汎用規約は リポジトリ直下の `CLAUDE.md` を参照。
================================================================================
-->

# C4モデル レベル1: システムコンテキスト

{{本図のスコープを1段落で明記: 本システムに直接情報を授受するアクター・外部システムのみを描き全アクターの網羅ではない旨と、間接関与で除外した業務関係者の例}}

```mermaid
flowchart LR
    classDef person fill:#08427b,color:#ffffff,stroke:#052e56;
    classDef system fill:#1168bd,color:#ffffff,stroke:#0b4884;
    classDef extsys fill:#8c8c8c,color:#ffffff,stroke:#5c5c5c;

    subgraph {{利用者グループ(例: 社内・社外)}}
        direction TB
        {{actN}}["{{アクター名}}<br/>{{ACT-N}}"]
    end

    sys["<b>{{対象システム名}}</b><br/>{{システムの説明}}"]:::system

    subgraph {{外部システムグループ(例: 自社別システム・外部サービス)}}
        direction TB
        {{extN}}["{{外部システム名}}<br/>{{EXT-N}}"]
    end

    {{actN}} -->|"{{利用目的}}"| sys
    sys -->|"{{連携内容}}"| {{extN}}

    class {{actN}} person;
    class {{extN}} extsys;
```

## 登場要素

{{システムと直接情報を授受する要素のみを掲載する旨を1行で記載}}

| ID | 種別 | 名称 | 説明・責務 | 参照元 |
|---|---|---|---|---|
| {{ACT-N}} | 人 | {{アクター名}} | {{役割・責務}} | アクター一覧 |
| — | 対象システム | {{対象システム名}} | {{システムの説明}} | PRD §プロダクト概要 |
| {{EXT-N}} | 外部システム | {{外部システム名}} | {{連携目的}} | 外部システム一覧 |