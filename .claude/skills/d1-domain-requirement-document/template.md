<!--
================================================================================
ドメイン要求仕様書 テンプレート

【使い方】
- 章構成は固定。1ドメイン = 1ファイル(ドメイン定義書の各ドメインに対応)
- 表記規約(プレースホルダ `{{xxx}}` / HINT記法 / 要確認マーカー / 文体 / `[フェーズ2]` ラベル / ID表記 等)はリポジトリ直下の `CLAUDE.md` を参照
- ID の成果物略号は **当該ドメインコード**(例: HEAR ドメインなら `DOM-HEAR-BR-1`)。カテゴリは BR(業務ルール)/ IRR(業務運用・イレギュラー)/ DATA(ドメイン固有データ)等
- 本書は専用の「対象ドメイン」「継承するPRD要求」「他ドメインとの連携」章を持たない。
  ドメイン定義は正典であるドメイン定義書を参照し、PRD横断要求の継承・他ドメイン連携は
  各業務ルールの「根拠/制約」列および「受け入れ基準」にインラインで紐づける
================================================================================
-->

# {{ドメイン名}}要求仕様書

## 本書について

### 概要

<!-- HINT: 定型。`{{ドメイン名}}` のみ置換する(ドメイン定義書の一覧名と一致させる)。 -->

本書は、[ドメイン定義書](../domain-definition-document#一覧)に記載されるドメインのうち、「{{ドメイン名}}」に関する要求事項を記載したドキュメントです。
本書は「本ドメインとして何を満たすべきか(What)」を扱います。

### 注記

<!-- HINT: 定型。置換不要。 -->

本書では原則として 具体的な実装手段(How)には踏み込みませんが、 **ビジネス・規制上譲れない本ドメイン固有のHow** は本書で確定します。

## 業務要求

### 業務ルール

<!-- HINT:
本ドメインの中核。業務として満たすべき判断基準・制約・条件分岐 を列挙する。
「誰が・いつ・何を・どういう条件で」を業務言語で記述。機能名・画面名・テーブル名・API 名は書かない。
本書は専用の継承章を持たないため、ドメイン共通要求(`DOM-COMMON-FR/NFR/EXT/SEC/SEC-DATA/REG-*`)や
他ドメイン連携・ドメイン定義書の関心事は「根拠/制約」列に ID 等で紐づけ、本書のトレーサビリティを担保する。
本ドメインの**責務境界**(他ドメイン・既存システムとの分担)が明確な場合は、責務範囲を業務ルールとして明示する
(例: 計上完了までを担い、以降は既存契約管理システム / 第一回収納まで担い継続収納は既存収納システム 等)。
将来段階導入の業務ルールは `[フェーズ2]` ラベルで初期/将来の境界を明示。
ID は `<ドメインコード>-BR-<連番>` 形式(例: DOM-HEAR-BR-1)。
導入文の例: 「本ドメイン固有の業務として満たすべき判断基準・制約・条件を以下に示します。」(規制対応色の強いドメインでは
「プロダクト横断で共通の要求は PRD を正典とし、ここでは再定義しません。」を併記してもよい)
-->

本ドメイン固有の業務として満たすべき判断基準・制約・条件を以下に示します。

| ID | 業務ルール | 内容 | 根拠/制約 |
|---|---|---|---|
| {{ID(例: <DOM>-BR-1)}} | {{ルール名}} | {{業務上満たすべき条件・判断基準}} | {{法令 / PRD §ID / ドメイン定義書の関心事・連携 等}} |

### 業務状態遷移

<!-- HINT:
本ドメインが管理する主要な業務対象(例: 申込・査定・収納 等)の業務状態と遷移を示す。
導入文では**主要業務対象を「」付きで明示**する(例:「主要な業務対象である『申込』の業務状態と遷移を示します」)。
独立した複数の業務対象を持つドメイン(例: AUDIT=アクセス権限+電子帳簿)は、Mermaid の `state "名前" as エイリアス { ... }` で
**composite state(複合状態)** にまとめ、対象ごとの状態機械を1つの図に並置する。
凡例の矢印記法は下のコードブロック側に記述し、HINT 内では「実線矢印=正常遷移」等と説明的に書く(HTMLコメントは最初の閉じ記号で終了するため、矢印記号を HINT 内に直接書かない)。
遷移ラベルに `(XXX-BR-N)` 等の業務ルールIDを括弧書きで添えると、図と業務ルール表の追跡が容易になる。
状態の定義表と遷移表(契機・主体・前提条件)を併記すると後続の L2 シーケンスが描きやすい。
詳細なシーケンスは下流の L1/L2 シーケンス図に降ろし、本書は状態と遷移の輪郭に留める。
-->

本ドメインが管理する主要な業務対象である「{{主要業務対象名}}」の業務状態と遷移を示します。

```mermaid
stateDiagram-v2
    [*] --> {{初期状態}}
    {{初期状態}} --> {{次状態}}: {{遷移契機}}({{XXX-BR-N}})
```

| 業務状態 | 定義 | この状態での主な制約 |
|---|---|---|
| {{状態名}} | {{状態の意味}} | {{当該状態で許容/禁止される業務操作}} |

| 遷移元 | 遷移先 | 契機 | 主体 | 前提条件 |
|---|---|---|---|---|
| {{状態}} | {{状態}} | {{何が起きたら遷移するか}} | {{誰が}} | {{遷移の前提}} |

### 業務運用(イレギュラー対応)

<!-- HINT:
正常系から外れる業務局面(不備・差し戻し・取消・タイムアウト・外部サービス不達・本人不在 等)と、その業務上の取り扱いを示す。
システム例外ではなく「業務としてどう扱うか」を記述する(D1成果物ではシステム寄り用語に「業務」を冠する)。
「業務上の対応」列の末尾に、関連業務ルール・他IRR・PRD要求のIDを `(XXX-BR-N に整合)` / `(DOM-COMMON-NFR-9 に整合)` 形式で
括弧書きし、対応の根拠と整合先を可視化する(トレーサビリティ強化)。
外部サービス連携が中核のドメイン(反社チェック・電子署名・決済 等)では、節の導入文に「外部不達・保留・再実施 等の
業務運用を厚く定めます」のような注記を1文添えてもよい。
ID は `<ドメインコード>-IRR-<連番>` 形式(例: DOM-HEAR-IRR-1)。
-->

正常系から外れる業務局面と、その業務上の取り扱いを以下に示します。

| ID | イレギュラー事象 | 発生契機 | 業務上の対応 |
|---|---|---|---|
| {{ID(例: <DOM>-IRR-1)}} | {{事象}} | {{何をきっかけに発生するか}} | {{業務としてどう収束させるか}} |

## セキュリティ要求

### データアクセス要求

<!-- HINT:
PRD §「セキュリティ要求 > データアクセス要求」の機密区分を継承しつつ、本ドメイン固有の主要データを詳細化する。
構造詳細(項目定義・型・カーディナリティ)は D2 のデータモデルに降ろす。本書は「本ドメインが扱う主要データと、その PRD 機密区分との対応・本ドメイン固有の取扱」レベルに留める。
ID は `<ドメインコード>-DATA-<連番>` 形式(例: DOM-HEAR-DATA-1)。
導入文(下記の1行)は**省略可**。表の「PRD 機密区分との対応」列がPRD継承を示すため、実プロジェクトの大半では見出し直後に表を置く形でよい。
PRD継承の文脈を明示したい場合のみ残す。
-->

PRD §「セキュリティ要求 > データアクセス要求」の機密区分を継承し、本ドメイン固有の主要データを以下に対応づけます。

| ID | データ | PRD 機密区分との対応 | 本ドメインでの取り扱い |
|---|---|---|---|
| {{ID(例: <DOM>-DATA-1)}} | {{データ名}} | {{DOM-COMMON-SEC-DATA-n / 区分}} | {{本ドメイン固有の保持・参照・更新の方針}} |

## 受け入れ基準

<!-- HINT:
本ドメインとして満たされなければ完成と見なさない基準を3〜7項目で示す。
継承した PRD 要求の本ドメインでの充足、業務ルールの網羅、主要業務状態遷移の正常/異常の通し確認 等。
専用の継承章を持たないため、継承 PRD 要求の充足確認は次のいずれかで担保する:
  - 形式A: 「継承 PRD 要求の充足: ...」として独立した1項目を立てる(汎用)
  - 形式B: 各ドメイン固有項目の末尾に `(DOM-COMMON-REG-1 / DOM-COMMON-REG-6 充足)` のように **インラインでPRD ID併記**(規制対応色が強く項目とPRDが1対1で対応するドメイン向け)
本書冒頭に項目を1つ立てる形式Aと、各項目末尾に併記する形式Bは併用可。いずれの形式でも、本書が継承するPRD要求が
受け入れ基準のどこかで漏れなく充足確認されることを担保する。
個別機能・画面の受け入れは下流(機能一覧・テスト計画)に降ろす。
-->

* {{受け入れ基準1}}: {{基準内容}}
* {{受け入れ基準2}}: {{基準内容}}
* {{受け入れ基準3}}: {{基準内容}}
