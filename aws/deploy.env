# KARURA サンプル成果物 Wiki — S3 デプロイ設定(非機密値のみ)
#
# scripts/deploy-s3.sh が読み込む。GitHub Actions 側は同名の Variables
# (S3_BUCKET / AWS_REGION)を Settings に登録し、値をここと揃える。
# アクセスキー等の機密は絶対にここへ書かない(OIDC のため長期キー自体が不要)。

BUCKET=karura-sample-life-insurance-artifacts
REGION=ap-northeast-1
